pandas==2.3.3
geopandas==1.1.1
folium==0.20.0
gradio==5.49.1
matplotlib==3.8.2

# Required for pandas.read_excel on .xlsx files
openpyxl==3.1.5

# Optional/related deps (installed automatically when creating the venv above):
# pyproj, shapely, pyogrio, numpy, pandas dependences are already pulled in.